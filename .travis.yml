language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t bugrauluyurt/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bugrauluyurt/docker-react npm run test -- --coverage